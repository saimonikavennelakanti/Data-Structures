INPUT:
#include<stdio.h>
int main()
{
	int a[10],i,even=0,odd=0;
	printf("Enter the elements:");
	for(i=0;i<10;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("Even numbers=");
		for(i=0;i<10;i++)
		{
	if(a[i]%2==0)
	{
		printf("%d",a[i]);
	}
}
	printf("Odd numbers=");
	for(i=0;i<10;i++)
		{
	if(a[i]%2!=0)
	{
		printf("%d",a[i]);
		}
	}
		return 0;
	}

 OUTPUT:
 Enter the elements:1 2 3 4 5 6 7 8 9 0
Even numbers=24680Odd numbers=13579
